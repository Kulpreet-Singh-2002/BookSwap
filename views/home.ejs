<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BookSwap | Home</title>

    <!-- Fonts + Bootstrap -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom Styling -->
    <link rel="stylesheet" href="/stylesheets/home.css">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background: #121212;
            color: #f8f9fa;
            height: 100vh;
            background-image: linear-gradient(rgba(0, 0, 0, 0.472), rgba(0, 0, 0, 0.5)),
                url("https://plus.unsplash.com/premium_photo-1721762724239-e5fbfc8ef3c1?w=500&auto=format&fit=crop&q=60&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxzZWFyY2h8MTA0fHxib29rfGVufDB8fDB8fHww");
            background-size: cover;
            background-position: center;
            text-shadow: 0 0.05rem 0.1rem rgba(0, 0, 0, 0.5);
            box-shadow: inset 0 0 5rem rgba(0, 0, 0, 0.5);
        }

        .cover-container {
            min-height: 100vh;
        }

        .nav-masthead .nav-link {
            color: rgba(255, 255, 255, 0.75);
            margin-left: 1rem;
        }

        .nav-masthead .nav-link.active,
        .nav-masthead .nav-link:hover {
            color: #fff;
            font-weight: 500;
        }

        .lead.mytext {
            font-size: 1.25rem;
            margin-top: 1rem;
        }

        footer p {
            margin-top: 2rem;
            font-size: 0.9rem;
        }

        h1 strong {
            font-size: 3rem;
        }
    </style>
</head>

<body class="d-flex text-center">
    <div class="cover-container d-flex w-100 h-100 p-3 mx-auto flex-column">
        <header class="mb-auto">
            <div class="d-flex justify-content-between align-items-center">
                <h3 class="mb-0">📚 BookSwap</h3>
                <nav class="nav nav-masthead justify-content-center">
                    <a class="nav-link active" href="#">Home</a>
                    <a class="nav-link" href="/books">Find Books</a>
                    <% if(!currentUser) { %>
                        <a class="nav-link" href="/login">Login</a>
                        <a class="nav-link" href="/register">Register</a>
                        <% } else { %>
                            <a class="nav-link" href="/logout">Logout</a>
                            <% } %>
                </nav>
            </div>
        </header>

        <main class="px-3 my-auto">
            <h1><strong>BookSwap</strong></h1>
            <p class="lead mytext">
                <b>Welcome to BookSwap!</b><br>
                One book out, a new story in.<br>
                Give books a second life — and find your next favorite read.
            </p>
            <a href="/books" class="btn btn-lg btn-warning text-dark fw-bold shadow-sm mt-3">📖 Browse Books</a>
        </main>

        <footer class="mt-auto text-white-50">
            <p>&copy; BookSwap 2025. All rights reserved.</p>
        </footer>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>